============
Installation
============

At the command line::

    $ easy_install bundleparser

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bundleparser
    $ pip install bundleparser
